---
import { Image } from 'astro:assets';
import { Icon } from 'astro-icon/components';
import myProfilePic from "../assets/davi.jpg"; 
import ThemeToggle from './ThemeToggle.astro';
---

<aside class="profile-card" aria-label="Perfil de Davi Carneiro">
  <div class="card-glow" aria-hidden="true"></div>
  <div class="card-surface">
    <span class="badge">Professor & Desenvolvedor</span>
    <a href="/" class="profile-pic-link" aria-label="Ir para a página inicial">
      <Image
        src={myProfilePic}
        alt="Retrato de Davi Carneiro"
        class="profile-pic"
        width={180}
        height={180}
        format="webp"
      />
    </a>

    <h1>Davi Carneiro</h1>
    <p class="headline">Explorando ciência, código e educação.</p>

    <nav class="main-nav" aria-label="Navegação principal">
      <a href="/">Início</a>
      <a href="/blog">Blog</a>
      <a href="/contato">Contato</a>
    </nav>

    <div class="meta-grid">
      <div>
        <span class="meta-label">Áreas</span>
        <strong>IA • Educação • Web</strong>
      </div>
      <div>
        <span class="meta-label">Base</span>
        <strong>Fortaleza, Brasil</strong>
      </div>
    </div>

    <div class="social-nav" aria-label="Redes sociais">
      <a href="https://github.com/..." target="_blank" rel="noopener noreferrer" aria-label="Github"><Icon name="mdi:github" /></a>
      <a href="https://linkedin.com/..." target="_blank" rel="noopener noreferrer" aria-label="LinkedIn"><Icon name="mdi:linkedin" /></a>
      <a href="https://x.com/..." target="_blank" rel="noopener noreferrer" aria-label="X"><Icon name="mdi:twitter" /></a>
      <a href="https://researchgate.net/..." target="_blank" rel="noopener noreferrer" aria-label="ResearchGate"><Icon name="mdi:school-outline" /></a>
      <a href="https://lattes.cnpq.br/..." target="_blank" rel="noopener noreferrer" aria-label="Lattes"><Icon name="mdi:account-box-outline" /></a>
    </div>

    <div class="divider" aria-hidden="true"></div>
    <ThemeToggle />
  </div>
</aside>

<style>
  .profile-card {
    position: fixed;
    top: var(--shell-padding);
    left: var(--shell-padding);
    width: min(var(--card-width), calc(100vw - 2 * var(--shell-padding)));
    max-width: 360px;
    z-index: 10;
    pointer-events: none;
  }

  .card-surface {
    position: relative;
    pointer-events: auto;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.25rem;
    padding: clamp(1.75rem, 2vw + 1.25rem, 2.5rem);
    border-radius: 24px;
    background: color-mix(in oklab, var(--color-bg) 85%, transparent);
    border: 1px solid color-mix(in oklab, var(--color-border) 60%, transparent);
    box-shadow: var(--shadow-card);
    backdrop-filter: blur(18px);
  }

  .card-glow {
    position: absolute;
    inset: -18%;
    border-radius: 32px;
    background: radial-gradient(circle at 20% 20%, rgba(113, 186, 255, 0.35), transparent 55%),
                radial-gradient(circle at 80% 30%, rgba(147, 91, 255, 0.32), transparent 60%);
    filter: blur(8px);
    opacity: 0.8;
    z-index: -1;
  }

  .badge {
    align-self: flex-start;
    font-size: 0.75rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    font-weight: 600;
    padding: 0.35rem 0.75rem;
    border-radius: 999px;
    background: linear-gradient(135deg, rgba(62, 122, 235, 0.18), rgba(158, 96, 255, 0.18));
    color: var(--color-text);
    border: 1px solid color-mix(in oklab, var(--color-border) 40%, transparent);
  }

  .profile-pic-link {
    display: inline-flex;
    border-radius: 50%;
    padding: 6px;
    background: radial-gradient(circle at 30% 30%, rgba(255, 255, 255, 0.7), rgba(255, 255, 255, 0));
    transition: transform 200ms ease, box-shadow 200ms ease;
  }

  .profile-pic-link:hover {
    transform: translateY(-4px) scale(1.01);
    box-shadow: 0 16px 40px rgba(64, 123, 255, 0.18);
  }

  .profile-pic {
    width: clamp(140px, 14vw + 60px, 180px);
    height: clamp(140px, 14vw + 60px, 180px);
    border-radius: 50%;
    border: 5px solid rgba(255, 255, 255, 0.9);
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.18);
  }

  h1 {
    font-size: clamp(1.75rem, 1.2vw + 1.6rem, 2rem);
    text-align: center;
  }

  .headline {
    text-align: center;
    color: color-mix(in oklab, var(--color-text) 85%, transparent);
    margin: 0;
    font-size: 0.95rem;
    max-width: 18ch;
  }

  .main-nav {
    display: flex;
    width: 100%;
    justify-content: center;
    gap: 0.75rem;
  }

  .main-nav a {
    flex: 1;
    text-align: center;
    text-decoration: none;
    font-weight: 600;
    padding: 0.65rem 0.75rem;
    border-radius: 12px;
    background: color-mix(in oklab, var(--color-bg) 70%, transparent);
    border: 1px solid color-mix(in oklab, var(--color-border) 60%, transparent);
    color: var(--color-text);
    transition: transform 200ms ease, box-shadow 200ms ease, border-color 200ms ease;
  }

  .main-nav a:hover {
    transform: translateY(-3px);
    box-shadow: 0 12px 24px rgba(61, 134, 255, 0.18);
    border-color: color-mix(in oklab, var(--color-primary) 55%, transparent);
  }

  .meta-grid {
    width: 100%;
    display: grid;
    gap: 0.75rem;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    text-align: left;
  }

  .meta-label {
    display: block;
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: color-mix(in oklab, var(--color-text) 65%, transparent);
    margin-bottom: 0.25rem;
  }

  .meta-grid strong {
    font-size: 0.95rem;
    font-weight: 600;
  }

  .social-nav {
    width: 100%;
    display: flex;
    justify-content: center;
    gap: 0.85rem;
  }

  .social-nav a {
    width: 42px;
    height: 42px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 12px;
    border: 1px solid color-mix(in oklab, var(--color-border) 50%, transparent);
    color: var(--color-text);
    background: color-mix(in oklab, var(--color-bg) 70%, transparent);
    transition: transform 200ms ease, box-shadow 200ms ease, color 200ms ease;
  }

  .social-nav a:hover {
    transform: translateY(-4px);
    box-shadow: 0 16px 32px rgba(77, 140, 255, 0.2);
    color: var(--color-primary);
  }

  .social-nav :global(svg) {
    width: 22px;
    height: 22px;
  }

  .divider {
    width: 100%;
    height: 1px;
    background: linear-gradient(90deg, transparent, color-mix(in oklab, var(--color-border) 70%, transparent), transparent);
  }

  :global(.theme-toggle) {
    width: 100%;
    justify-content: center;
  }

  @media (max-width: 1060px) {
    .profile-card {
      position: static;
      width: 100%;
      max-width: none;
      margin: 0 auto 2rem;
      pointer-events: auto;
    }

    .card-surface {
      align-items: center;
    }

    .meta-grid {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .profile-pic-link:hover {
      transform: translateY(-2px) scale(1.01);
    }
  }

  @media (max-width: 768px) {
    .profile-card {
      margin-top: 1rem;
      padding: 0 clamp(1rem, 4vw, 1.5rem);
    }

    .card-surface {
      border-radius: 20px;
    }

    .main-nav {
      flex-direction: column;
    }

    .main-nav a {
      width: 100%;
    }

    .social-nav {
      flex-wrap: wrap;
    }
  }
</style>