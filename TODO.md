# Pendencias

- Migrar de Astro 5 com `@astrojs/cloudflare` 12 para Astro 7 com `@astrojs/cloudflare` 14, validar o runtime no Cloudflare e revisar as vulnerabilidades do `npm audit`.
- Configurar um remetente personalizado verificado no Resend usando `RESEND_FROM`, substituindo o fallback `onboarding@resend.dev`.
- Confirmar e remover, se ainda existir, qualquer projeto ou integração histórica da Vercel na conta externa.
